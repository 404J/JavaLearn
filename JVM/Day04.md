# Day04

## 对象的内存布局

### 对象的创建过程

- 如果 class 未加载，首先加载
- 申请对象内存
- 成员变量赋默认值
- 调用构造方法：成员变量赋初始值，执行构造方法语句

### 对象在内存中的存储布局

- 普通对象
  1. 对象头 markword 8个字节
  2. classpoint 指针，指向 class 对象
  3. 实例数据
  4. 对齐为8的倍数

- 数组对象
  1. 对象头 markword 8个字节
  2. classpoint 指针，指向 class 对象
  3. 数组的长度
  4. 数组的数据
  5. 对齐

- 对象头包含内容
  1. 
